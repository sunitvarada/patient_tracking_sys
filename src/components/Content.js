import * as React from 'react';
import {Patient} from "./Patient/AddPatient";

const Content = () => {
    return (
        <div>
            <Patient/>
        </div>
    );
};

export default Content